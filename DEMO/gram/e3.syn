%ignore	' ';
@int 	/[0-9]+/;

factor	: '(' expr ')'
		| int ;

term 	: @mul( term '*' factor )
		| @div( term '/' factor )
		| factor
		;

@add 	: expr '+' term;
@sub 	: expr '-' term;

expr$	: add | sub | term ;

