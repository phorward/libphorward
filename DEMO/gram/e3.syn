%whitespace ' ' 

int = /[0-9]+/ %emit;

f: '(' e ')' | int ;

mul : t '*' f %emit %id 1;
div : t '/' f %emit %id 2;
t : mul | div | f ;

add : e '+' t %emit %id 3;
sub : e '-' t %emit %id 4;

e: add | sub | t %goal;

